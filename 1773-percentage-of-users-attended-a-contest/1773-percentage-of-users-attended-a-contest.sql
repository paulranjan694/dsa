# Write your MySQL query statement below
select r.contest_id, Round((count(r.user_id)/u.total * 100),2) as percentage from Register r join (select count(*) as total from Users) as u group by r.contest_id order by percentage desc, r.contest_id asc;